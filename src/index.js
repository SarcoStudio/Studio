import React from "react";
import ReactDOM from "react-dom";
import "./index.css"; // You can create this later if needed
import App from "./App"; // This will be your main app component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
